<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Run.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Run"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Run">
    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Description Text and Logo -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,-10,10,0">
            <Image Width="40" Source="Assets/Logo.ico"/>
            <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="15,10,15,10" FontSize="13">
                Type the name of a program, folder, document, or
                <LineBreak/>
                Internet resource, and Windows will open it for you.
            </TextBlock>
        </StackPanel>

        <!-- Input Field -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="12,0,0,0">
            <TextBlock Text="Open:" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <TextBox x:Name="txtRunCommand" KeyDown="TxtRunCommand_KeyDown" TextChanged="TxtRunCommand_TextChanged" Loaded="TxtRunCommand_Loaded" PlaceholderText="Enter a command" Width="300" Height="32"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="btnOk" Content="OK" Width="75" Margin="10,0,0,0" Click="BtnOk_Click" IsEnabled="False"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="75" Margin="10,0,0,0" Click="BtnCancel_Click"/>
            <Button x:Name="btnBrowse" Content="Browse..." Width="75" Margin="10,0,0,0" Click="BtnBrowse_Click"/>
        </StackPanel>
    </Grid>
</Window>
